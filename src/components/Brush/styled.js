import styled from "styled-components";

const StyledBrush = styled.div`
  max-height: 20rem;
  img {
    height: 100%;
    width: 100%;
    object-fit: contain;
  }
`;

export default StyledBrush;
