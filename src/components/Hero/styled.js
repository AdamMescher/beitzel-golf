import styled from "styled-components";

const StyledHero = styled.section`
  width: calc(100% - 5rem);
  padding: 0 0 0 5rem;
  overflow-x: hidden;
  overflow: hidden;
  margin-top: 3rem;
`;

export default StyledHero;
